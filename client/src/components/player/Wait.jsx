import React from 'react';
//import /*component name*/ from './childname.jsx'; if needed

//TODO will have state, needs to be ES6 Class


// Make a component to render the game players's join screen
const Wait = (props) => (
  <div>
    Wait for all players to join.
  </div>
);

export default Wait;